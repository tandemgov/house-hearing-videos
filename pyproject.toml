[project]
name = "house-hearing-videos"
version = "0.1.0"
description = "Crosswalk matching House hearings to YouTube videos"
requires-python = ">=3.11"
license = "CC0-1.0"
dependencies = [
    "httpx>=0.27",
    "polars>=1.0",
    "thefuzz>=0.22",
    "python-dotenv>=1.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "ruff>=0.8",
    "respx>=0.22",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
